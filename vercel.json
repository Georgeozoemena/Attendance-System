{
  "buildCommand": "cd frontend && npm install && npm run build && rm -rf ../public && mv dist ../public",
  "outputDirectory": "public",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
